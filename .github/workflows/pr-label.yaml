name: Use Common Workflow

on:
  pull_request:
    types: [opened]

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  call-common:
    uses: narumimashiro/akashic-records/.github/workflows/tag.yml@main